#!/bin/bash

set -euo pipefail

set -vx

if [ "${COVERAGE}z" != z ]; then
    echo building with coverage on sonarcloud
    mvn test -P${CDI} jacoco:report sonar:sonar
else
    echo building without coverage
    mvn test -P${CDI}
fi